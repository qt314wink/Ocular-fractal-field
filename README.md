# Ocular Fractal Field

Framework-neutral TypeScript is the editable production authority. Standalone HTML is generated output, and canonical simulation authority remains isolated from rendering, audio, and input adapters.

The approved design and foundation authorities are stored in [`docs/authority/`](docs/authority/). The source manifest records the independently verified, user-approved reconstructed production-design PDF and preserves its superseded expected hash as provenance.
